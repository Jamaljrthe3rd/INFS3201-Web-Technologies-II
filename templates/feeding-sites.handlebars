<!DOCTYPE html>
<html>
<head>
    <title>Stray Cat Feeding Stations</title>
    <style>
        .site-card {
            border: 1px solid #ddd;
            padding: 15px;
            margin: 10px 0;
            border-radius: 5px;
            background-color: white;
        }
        .status-info {
            display: flex;
            gap: 20px;
            margin-top: 10px;
        }
        .status-item {
            background: #f8f9fa;
            padding: 5px 10px;
            border-radius: 3px;
        }
    </style>
</head>
<body>
    <h1>Stray Cat Feeding Stations in Qatar</h1>
    
    <div class="sites-container">
        {{#if sites}}
            {{#each sites}}
                <div class="site-card">
                    <h2>{{name}}</h2>
                    <p>{{description}}</p>
                    <div class="status-info">
                        <span class="status-item">Food Level: {{foodLevel}}</span>
                        <span class="status-item">Water Level: {{waterLevel}}</span>
                        <span class="status-item">Cats Present: {{catCount}}</span>
                        <span class="status-item">Last Visit: {{formatDate lastVisit}}</span>
                    </div>
                </div>
            {{/each}}
        {{else}}
            <p>No feeding stations have been registered yet.</p>
        {{/if}}
    </div>

    {{#unless user}}
        <div class="signup-prompt" style="margin-top: 20px; padding: 15px; background: #f8f9fa; border-radius: 5px;">
            <h3>Want to help?</h3>
            <p>Sign up to record your visits and help keep our feline friends fed!</p>
            <a href="/register" class="btn btn-primary">Sign Up Now</a>
        </div>
    {{/unless}}
</body>
</html>
